function Mainsection({children}: {children?: React.ReactNode}) {
    
    return (
        <div className = "main-section-main-screen">
            {children}
        </div>
    )
}

export default Mainsection;